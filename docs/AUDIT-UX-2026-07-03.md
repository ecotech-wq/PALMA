# Audit UX (2026-07-03) : 5 parcours, 63 defauts releves

Genere par le workflow d'audit (parcours chef, conducteur, admin, client, navigation).
A VERIFIER un par un avant correction : certains constats d'agents sont faux
(ex. le backHref de la fiche chantier existe deja).
Statuts : A VERIFIER / CONFIRME / REJETE / CORRIGE (hash).

## 1. [P1-terrain] Suppression de compte sans confirmation modale — utilise confirm() JavaScript natif
- Source : parcours admin ; fichier : src/app/(app)/admin/users/UserActions.tsx
- Scenario : Admin clique sur le bouton Supprimer (poubelle) pour un utilisateur. Une popup JavaScript confirm() apparaît, mais sur téléphone c'est clair et l'utilisateur peut supprimer accidentellement en validant.
- Correction proposee : Remplacer le confirm() JavaScript par un DialogConfirm React/composant modale réutilisable avec boutons clairs (Annuler, Supprimer). La modale doit afficher le nom de l'utilisateur et ses conséquences.
- Statut : CONFIRME
- Contre-expertise : C:/Projects/outil-gestion-chantier/src/app/(app)/admin/users/UserActions.tsx:307 utilise if (!confirm(...)) pour la suppression de compte. C'est un JavaScript natif confirm(), pas une modale React. Le scénario utilisateur est valide : admin clique le bouton danger (icône poubelle seule, sans libellé texte), une popup système s'affiche, peu ergonomique sur téléphone.
- Correction validee : Remplacer le confirm() natif (ligne 307) par une modale React DialogConfirm réutilisable. La modale doit afficher : titre "Supprimer le compte", description avec le nom de l'utilisateur, deux boutons "Annuler" et "Supprimer" clairs. Ajouter le libellé texte au bouton poubelle (icône + "Supprimer").

## 2. [P1-terrain] Retrait de membre du chantier sans confirmation modale — utilise confirm() natif ligne 62
- Source : parcours admin ; fichier : src/features/membership/components/MembresCard.tsx
- Scenario : Admin/Conducteur clique sur la poubelle pour retirer un membre du chantier. Le confirm() natif s'affiche, peu visible sur téléphone, membre retiré de tous les canaux. L'utilisateur (chef, conducteur) perd accès au chantier sans avertissement clair.
- Correction proposee : Remplacer confirm() par DialogConfirm avec message explicite : 'Retirer [NOM] du chantier et de ses canaux ? Cette action est irréversible.'
- Statut : CONFIRME
- Contre-expertise : C:/Projects/outil-gestion-chantier/src/features/membership/components/MembresCard.tsx:62 — `if (!confirm(...))` utilise confirm() natif JavaScript. La fonction removeChantierMembre supprime bien le membre de tous les canaux (confirme par membre-actions.ts:78-81 qui efface canalMembre en cascade). Aucun DialogConfirm composant React n'existe dans le codebase.
- Correction validee : Remplacer le confirm() natif ligne 62 par un composant DialogConfirm React réutilisable. Créer src/components/DialogConfirm.tsx qui accepte (titre, message, onConfirm, onCancel) et affiche une modale avec boutons clairs (Annuler / Confirmer). Puis utiliser cet état dans MembresCard via un useState([open, setOpen]) qui enveloppe l'appel à removeChantierMembre.

## 3. [P1-terrain] Suppression de chantier sans confirmation modale — bouton poubelle (ligne 161-166)
- Source : parcours admin ; fichier : src/app/(app)/chantiers/[id]/page.tsx
- Scenario : Admin accède à la fiche chantier et clique le bouton Supprimer (poubelle). Aucune confirmation n'apparaît : le chantier et tous ses pointages/commandes/rapports sont supprimés instantanément.
- Correction proposee : Ajouter une form action qui déclenche un DialogConfirm confirmant : 'Supprimer le chantier [NOM] et toutes ses données (pointages, commandes, rapports) ? Cette action est définitive.'
- Statut : CONFIRME
- Contre-expertise : C:/Projects/outil-gestion-chantier/src/app/(app)/chantiers/[id]/page.tsx:160-167 — Bouton de suppression sans confirmation. La form action déclenche deleteChantier() directement (C:/Projects/outil-gestion-chantier/src/app/(app)/chantiers/actions.ts:76-81) sans aucun dialogue de confirmation modale ou JavaScript natif. L'utilisateur clique le bouton poubelle et le chantier est supprimé instantanéme
- Correction validee : Ajouter une modale de confirmation AVANT la soumission de la form. Implémenter un DialogConfirm component (React client) qui demande : 'Supprimer le chantier [NOM] et toutes ses données (pointages, commandes, rapports) ? Cette action est définitive.' avec boutons Annuler/Supprimer. Lier le bouton Supprimer de la page à onSubmit du formulaire pour afficher la modale avant d'exécuter la server actio

## 4. [P1-terrain] Retrait d'équipe du chantier sans confirmation — bouton text 'Retirer' (ligne 262-269)
- Source : parcours admin ; fichier : src/app/(app)/chantiers/[id]/page.tsx
- Scenario : Admin retire une équipe du chantier en cliquant 'Retirer' (lien text à droite d'une équipe affectée). Aucune confirmation : l'équipe et tous ses ouvriers perdent le lien au chantier instantanément. Problème de lisibilité sur mobile.
- Correction proposee : Convertir le bouton text en Button réactif avec onClick déclenchant un DialogConfirm : 'Retirer l'équipe [NOM] du chantier ?'
- Statut : CONFIRME
- Contre-expertise : Code: C:/Projects/outil-gestion-chantier/src/app/(app)/chantiers/[id]/page.tsx:264-271 — Le bouton "Retirer" une équipe est un simple &lt;form action={detach}&gt; + &lt;button type="submit"&gt; sans DialogConfirm, sans confirmation modale. Action serveur affecterEquipeAuChantier (C:/Projects/outil-gestion-chantier/src/app/(app)/chantiers/actions.ts:108-116) exécute directement sans demander confir
- Correction validee : Importer DialogConfirm (à créer si inexistant, sinon utiliser un pattern de confirmation) et envelopper la form action du bouton "Retirer" d'une modale. Convertir le bouton text en Button reactif avec onClick déclenchant DialogConfirm : 'Retirer l'équipe [NOM] du chantier ?' Ou ajouter un confirm() JavaScript natif comme pattern temporaire (moins bon sur mobile, mais mieux qu'aucune confirmation).

## 5. [P1-terrain] Suppression d'un paiement : bouton sans confirmation modale — destructive action
- Source : parcours admin ; fichier : src/app/(app)/paie/[id]/page.tsx
- Scenario : Admin consulte un paiement (page 263-286). Clique 'Supprimer ce paiement' (bouton rouge). Aucune modale de confirmation : paiement et ses avances/retenues sont supprimés immédiatement.
- Correction proposee : Ajouter DialogConfirm avant deleteAction : 'Supprimer ce paiement de [MONTANT] € pour [OUVRIER] ? Les avances réglées seront restaurées.'
- Statut : CONFIRME
- Contre-expertise : src/app/(app)/paie/[id]/page.tsx:269-278 — Le bouton "Supprimer ce paiement" fait un form submit direct vers la server action deletePaiement (lignes 309-360 de src/app/(app)/paie/actions.ts) sans aucune confirmation modale, confirm() JavaScript, ou autre mécanisme de vérification. La suppression détruit le paiement et restaure les avances/retenues de manière irréversible sans feedback utilisateur 
- Correction validee : Ajouter une modale de confirmation avant la suppression. Option 1 : Créer un composant DialogConfirm React réutilisable (comme préconisé dans le constat 6). Option 2 : Au minimum, ajouter if (!confirm("Supprimer ce paiement de X € pour [OUVRIER] ? Les avances réglées seront restaurées.")) return; dans le formulaire via une action intermédiaire côté client. Option 3 (meilleur UX) : Remplacer le for

## 6. [P1-terrain] Actions destructives (retrait équipe, suppression compte) utilisent confirm() — pas visible sur téléphone
- Source : parcours admin ; fichier : src/app/(app)/paie/PaiePendingList.tsx
- Scenario : C'est un symptôme général : app utilise confirm() pour ~12 actions destructives (voir grep). Sur téléphone, les popups JavaScript sont peu contrastées, peu accessibles (pas clavier), et facilement cliquées par erreur dans le chaos d'une journée de chantier.
- Correction proposee : Créer un composant DialogConfirm réutilisable, l'appliquer systématiquement à toutes les actions destructives. Script d'audit : remplacer tous les confirm() de src/app.
- Statut : CONFIRME
- Contre-expertise : Le code contient 20+ usages de confirm() JavaScript natif pour les actions destructives (suppression compte, retrait membre, suppression chantier, paiement, réserves, tâches, photos, messages, etc.). Exemples vérifiés : - UserActions.tsx:307 — confirm("Supprimer définitivement ce compte ?") - MembresCard.tsx:62 — confirm() pour retrait membre - ReserveList.tsx, ReserveForm.tsx — confirm() pour sup
- Correction validee : Créer un composant DialogConfirm réutilisable (ex: src/components/ui/DialogConfirm.tsx) avec modale React accessible. Remplacer les ~20 usages de confirm() et ajouter confirmation modale aux form actions (deleteChantier, deletePaiement). Pattern : onClick handler déclenche dialogOpen, bouton Confirmer exécute l'action.</correction> </invoke>

## 7. [P1-terrain] Rapport : 2 écrans au lieu de 1, perte de temps (terrain critique)
- Source : parcours chef ; fichier : src/app/(app)/rapports/nouveau/page.tsx + RapportForm.tsx
- Scenario : BEN veut poster son rapport du jour depuis le chantier. Il clique 'Nouveau rapport', arrive sur écran 1 : sélecteur de chantier. Il ne peut pas continuer sans cliquer 'Continuer →' (2e action). Puis écran 2 : le formulaire. Alors que depuis la fiche du chantier, il y a DEJA un formulaire en modal (logique). DEFAUT : 2 gestes inutiles + hop entre deux pages = friction sur le terrain.
- Correction proposee : Accéder au formulaire directement DEPUIS la fiche chantier (via modal/drawer), ou au minimum utiliser un formulaire sur UNE seule page /rapports/nouveau?chantierId=X avec le chantier pré-sélectionné et caché EN MOBILE. Les conducteurs y voient le sélecteur, les chefs non.
- Statut : CONFIRME
- Contre-expertise : Le code à C:/Projects/outil-gestion-chantier/src/app/(app)/rapports/nuevo/page.tsx (lignes 39-86) implémente bien un flux à 2 écrans : (1) Sélecteur de chantier (ligne 39-70) + bouton "Continuer →" (ligne 65), puis (2) Affichage du formulaire RapportForm (ligne 81). Le scénario utilisateur est exact : BEN clique "Nouveau rapport" depuis /rapports/page.tsx (ligne 85), arrive à screen 1 sans chantie
- Correction validee : Fusionner les deux écrans en UNE SEULE PAGE avec : 1) Si URL a chantierId → pré-remplir et masquer le sélecteur (chiefs), afficher formulaire direct. 2) Si pas de chantierId → afficher formulaire avec sélecteur pré-agrandi, pas de bouton "Continuer" séparé. Code source : refactoriser /rapports/nuevo/page.tsx pour afficher RapportForm directement en passant chantierId en defaultValue du select, san

## 8. [P1-terrain] Pointage jour : pas de moyen de pointer l'EQUIPE entière en 1 geste
- Source : parcours chef ; fichier : src/app/(app)/pointage/PointageGrid.tsx
- Scenario : BEN pointe les ouvriers un par un (case par case). Équipe de 5 ouvriers = 5 clics mini. Or les chefs pointent souvent UNE EQUIPE au complet (sur un même chantier, même nombre de jours). DEFAUT : il existe un bouton 'Marquer tout' par équipe, mais AUCUN moyen RAPIDE de dire 'tous 1 jour' depuis le dashboard.
- Correction proposee : Ajouter des boutons de bulk dès la vue jour : [½ j.] [1 j.] au-dessus de chaque équipe pour appliquer en une action à toute l'équipe. Ou checkboxes pour sélectionner plusieurs, puis action groupée en bas.
- Statut : NON VERIFIE (echec agent)

## 9. [P1-terrain] Champs de formulaire : text-base (16px) OK sur mobile, MAIS pas assez padding vertical
- Source : parcours chef ; fichier : src/components/ui/Input.tsx (ligne 5, text-base) + formulaires incidents/rapports
- Scenario : BEN le 4e jour en plein soleil tape sur le clavier avec des gants de travail. Les inputs ont py-2 = padding vertical 8px = target trop petit pour gants. DEFAUT : au moment où il tape, le clavier cache le haut du formulaire et il ne voit pas le label du champ où il tente de taper.
- Correction proposee : Augmenter à py-3 (12px) minimum sur mobile (utiliser @apply ou className conditionnel). Ajouter scroll-into-view côté client dans les inputs critiques (titre incident, description).
- Statut : CONFIRME
- Contre-expertise : src/components/ui/Input.tsx:5 contient `py-2` (8px padding vertical) au lieu du minimum tactile recommandé (44px cible). La ligne 5 : fieldClasses="... px-3 py-2 text-base ...". Aucune classe responsive pour mobile (pas de md:py-3 ou sm:py-3). src/app/(app)/layout.tsx:175 applique pb-24 systématiquement sur tous les contenus sans distinction formulaire/liste. scrollIntoView n'est pas implémenté da
- Correction validee : 1) src/components/ui/Input.tsx ligne 5 : remplacer `py-2` par `py-2 md:py-3` ou mieux `py-3`. 2) Ajouter `@apply focus:scroll-into-view` aux fieldClasses ou implémenter un onFocus hook dans Input avec scrollIntoView. 3) src/app/(app)/layout.tsx ligne 175 : conditionner pb-24 aux pages de liste uniquement, pas aux formulaires (utiliser une classe CSS ou provider pour les formulaires).

## 10. [P1-terrain] Demande de matériel : chantier NON pré-sélectionné depuis la fiche
- Source : parcours chef ; fichier : src/app/(app)/demandes/nouvelle/page.tsx
- Scenario : BEN est sur la fiche de son chantier, voit une section 'Demandes', clic 'Nouvelle demande' → navigue vers /demandes/nouvelle SANS chantierId=X. Il doit re-sélectionner le chantier avec les autres champs déjà remplis. DEFAUT : perte de contexte = 2 actions au lieu de 1.
- Correction proposee : Depuis la fiche chantier, utiliser le deeplink /demandes/nouvelle?chantierId=X et pré-remplir le champ (disabled ou juste readonly). Ou préférable : modal/drawer directement sur la fiche (comme rapports).
- Statut : REJETE
- Contre-expertise : Le constat 10 décrit un scenario où BEN serait sur une fiche chantier et verrait une section 'Demandes' avec un bouton 'Nouvelle demande' qui navigue sans chantierId=X. Or, le code actual ne contient AUCUNE section 'Demandes' sur la fiche chantier. La fiche est structurée en 4 onglets: Vue d'ensemble, Équipe, Documents, Finances (src/app/(app)/chantiers/[id]/page.tsx lignes 117-126). Les demandes 

## 11. [P1-terrain] Dashboard : 'À traiter' affiche incidents, demandes, messages MAIS jamais les demandes sorties/locations en retard
- Source : parcours chef ; fichier : src/app/(app)/dashboard/page.tsx
- Scenario : BEN regarde son dashboard (Accueil). Il voit 'À traiter' avec incidents, demandes, messages. MAIS il ne voit PAS les sorties matériel ouvertes depuis 30j (badge 'sorties' en retard existe en sidebar). Il ne sait pas qu'il faut aller dans Sorties pour marquer le retour. DEFAUT : action critique cachée (sorties = oubliées → vol / perte de matériel).
- Correction proposee : Ajouter une ligne 'À traiter' pour les sorties EN RETARD (dateRetour null et dateSortie > 30j) dans le dashboard. Ou au minimum : afficher le badge 'sorties' en retard AUSSI dans À traiter.
- Statut : CONFIRME
- Contre-expertise : Fichier C:/Projects/outil-gestion-chantier/src/app/(app)/dashboard/page.tsx lignes 169-229 : la section "À traiter" ajoute incidents (ligne 178), demandes (ligne 190), messages (ligne 200) et locations en retard (ligne 210), MAIS aucune sortie matériel en retard. Fichier C:/Projects/outil-gestion-chantier/src/app/(app)/layout.tsx lignes 80-88 : les sorties en retard (dateRetour NULL et dateSortie 
- Correction validee : Ajouter une requête de comptage des sorties en retard (dateRetour IS NULL ET dateSortie > 30 jours) dans le dashboard page.tsx (ligne 59, après locationsRetardCount), puis ajouter un bloc "sorties" dans le tableau aTraiter (après locations, ligne 220) avec titre, lien vers /sorties, et badge "traiter".

## 12. [P1-terrain] Lien 'incidents' sans vérification de visibility.showIncidents
- Source : parcours client ; fichier : src/app/(app)/dashboard/ClientDashboard.tsx (ligne 227)
- Scenario : Si un ADMIN désactive l'accès aux incidents pour un CLIENT (showIncidents=false), le CLIENT voit quand même le lien '/incidents/{id}' depuis le dashboard. Le clic marque un incident 'en cours' mais impossible de le voir (redirect vers dashboard). Message d'erreur réseau ou confusion.
- Correction proposee : Dans ClientDashboard.tsx, filtrer la liste incidents par me.visibility.showIncidents AVANT d'afficher le lien. Actuellement, visibilité est appliquée au niveau page (/incidents/page.tsx line 36) mais pas au dashboard.
- Statut : CONFIRME
- Contre-expertise : ClientDashboard.tsx (lignes 69-77) charge les incidents SANS vérifier me.visibility.showIncidents. La liste d'incidents est affichée aux lignes 214-249 sans filtre. La page détail /incidents/[id]/page.tsx n'a aucune vérification de showIncidents (grep showIncidents retourne néant). Scénario confirmé: CLIENT avec showIncidents=false peut voir le lien incidents depuis le dashboard et y accéder. La v
- Correction validee : Passer me.visibility (ou me.visibility.showIncidents) à ClientDashboard et filtrer la requête incidents avec: if (!me.visibility.showIncidents) return null ou masquer le bloc Card Incidents entièrement. Alternativement, ajouter un contrôle dans /incidents/[id]/page.tsx ligne 40 après requireAuth(): if (me.isClient && !me.visibility.showIncidents) { redirect('/dashboard'); }

## 13. [P1-terrain] CLIENT voit bloc 'Informations' au format read-only mais rencontre 'PV de réception' inaccessible via bouton
- Source : parcours client ; fichier : src/app/(app)/chantiers/[id]/page.tsx (lignes 172-189)
- Scenario : CLIENT ouvre un chantier. Voit boutons 'Journal', 'Plans', 'Rapport hebdo', 'PV réception' tous visible. Clique sur 'PV réception'. Si le CLIENT n'est pas admin ET qu'il n'y a pas de PV → PageHeader vague ou redirect silencieux. Pas clair que 'PV réception' est réservé au pilotage (admin + conducteur).
- Correction proposee : Masquer les boutons 'PV réception' et 'Plans' pour les CLIENTs (même si plans peuvent être lus : clarifier visibilité via `me.visibility.showPlans`). Actuellement 'plans' n'est pas caché du menu mais est restreint par showPlans.
- Statut : REJETE
- Contre-expertise : C:/Projects/outil-gestion-chantier/src/app/(app)/chantiers/[id]/page.tsx:311 — La section "Espaces du chantier" contenant les boutons "Journal", "Plans", "Rapport hebdo", "PV de réception" est restreinte aux non-clients via `{!me.isClient && onglet === "documents" && (...)`. Un CLIENT ne peut donc pas voir ces boutons du tout sur la fiche chantier. Le scénario du constat ("CLIENT voit boutons visi

## 14. [P1-terrain] CLIENT voit 'Budget' avec montants sur fiche chantier malgré canSeePrices=false
- Source : parcours client ; fichier : src/app/(app)/chantiers/[id]/page.tsx (lignes 169-176)
- Scenario : CLIENT accède à /chantiers/{id}. Le code affiche budget SEULEMENT si `me.canSeePrices` (ligne 169). Mais : ne pas afficher du tout vs afficher avec visibilité dépend de la logique backend. Risque : fuite de montants si un renderage échoue ou un flag oublié.
- Correction proposee : Sur ClientDashboard.tsx + page chantier : confirmer que budgets ne sont affichés QUE si canSeePrices=true, jamais même masqué visuellement (aucune indication chiffrée). Tester en tant que CLIENT : aucun € visible nulle part sauf s'il faut passer par les interfaces financières (inaccessibles via auth.config.ts).
- Statut : REJETE
- Contre-expertise : Le code est correctement protégé à trois niveaux :  1. Auth (canSeePrices: false pour CLIENT - auth-helpers.ts:108) 2. Template (affichage conditionnel !me.isClient && me.canSeePrices - chantiers/[id]/page.tsx:121, 392, 436, 495) 3. Composant (Montant masque visuellement en mode discret).  Les budgets dans ChantierForm (lignes 206-207) sont placés en hidden input (lignes 137-147) pour préserver lo

## 15. [P1-terrain] Pas de filtre par chantier sur la liste des demandes
- Source : parcours conducteur ; fichier : src/app/(app)/demandes/page.tsx
- Scenario : Le conducteur de travaux pilote 3 chantiers actifs. Il ouvre Demandes matériel : voit TOUS les demandes de TOUS les chantiers sans possibilité de filtrer par chantier. Doit manuellement parcourir 50+ demandes pour trouver ce qui le concerne. Sur mobile (375px), risque de confusion et temps perdu.
- Correction proposee : Ajouter un filtre par chantier (select en haut de page) qui restreint la liste aux demandes du chantier choisi. Syntaxe : ?chantier=ID passé dans les URL de FilterTab, et appliquer un where({ chantierId }) à la requête findMany.
- Statut : CONFIRME
- Contre-expertise : src/app/(app)/demandes/page.tsx:25-35 : searchParams n'accepte que { statut?: string }, pas de chantier. src/app/(app)/demandes/page.tsx:47-56 : findMany n'applique que where.statut, aucune condition chantierId. Un conducteur pilotant 3 chantiers voit TOUTES les demandes (50+) sans filtrage par chantier possible.
- Correction validee : Ajouter un paramètre chantier aux searchParams (ligne 28), puis dans le where object (ligne 37-45), ajouter : chantierId?: string accepté en URL, puis appliquer un where({ chantierId: selectedChantierId }) si l'utilisateur est conducteur non-admin. Ajouter un select/dropdown en haut de page pour choisir le chantier (lire depuis ChantierMembre si conducteur, sinon tous les chantiers).

## 16. [P1-terrain] Pas de filtre par chantier sur les commandes, pas de tri par date limite
- Source : parcours conducteur ; fichier : src/app/(app)/commandes/page.tsx
- Scenario : Conducteur cherche une commande urgente en retard de livraison (location + commande = risque financier). Les commandes sont listées par dateCommande DESC, sans date limite visible. Les retards de livraison (date limite < aujourd'hui) ne sont pas signalés en rouge ni triés en premier.
- Correction proposee : 1) Ajouter filtre par chantier (même logique que demandes). 2) Dans la requête, inclure dateLivraisonPrevue. 3) Afficher en haut un compteur d'alerte « N commandes en retard » avec badge rouge. 4) Trier par (retard DESC, dateLivraisonPrevue ASC).
- Statut : CONFIRME
- Contre-expertise : src/app/(app)/commandes/page.tsx:15-23 — requête excludes dateLivraisonPrevue, aucun filtre par chantier, tri par dateCommande DESC uniquement. Affichage (69-77) ne montre pas dateLivraisonPrevue ni badge d'alerte. Aucun compteur de retards. Scénario plausible : conducteur multi-chantiers ne peut pas filtrer ni identifier les commandes urgentes en retard.
- Correction validee : 1) Inclure dateLivraisonPrevue dans la requête findMany (ligne 15-23). 2) Ajouter searchParams filtre chantierId comme demandes/page.tsx. 3) Afficher dateLivraisonPrevue en métadonnée (ligne 74). 4) Calculer enRetard: dateLivraisonPrevue < today. 5) Ajouter compteur rouge en haut (AlertCircle + « N commandes en retard »). 6) Trier par (enRetard DESC, dateLivraisonPrevue ASC) plutôt que dateCommand

## 17. [P1-terrain] Pas de filtre par chantier sur les locations, impossible de rappeler le fournisseur en un clic
- Source : parcours conducteur ; fichier : src/app/(app)/locations/page.tsx
- Scenario : Conducteur doit contacter le fournisseur d'une location en retard. La liste globale inclut locations de 3 chantiers (impossible de trier). Le fournisseur (fournisseurNom) est affichée mais non-cliquable (pas de lien tel ou mail). Conducteur doit recopier le nom manuellement, chercher le tel ailleurs, perdre 2 min par location.
- Correction proposee : 1) Ajouter filtre par chantier (dropdown). 2) Dans LocationForm.tsx, stocker fournisseurTel (nouveau champ). 3) Afficher le fournisseur comme lien cliquable : <a href="tel:+33..." ou mailto: si mail>. 4) Afficher aussi un bouton « Appeler » qui passe le numéro au téléphone native.
- Statut : CONFIRME
- Contre-expertise : Code review : - Ligne 18-21 de /locations/page.tsx : query findMany() récupère TOUTES les locations sans filtre par chantier (where: { cloture: false } seulement). Aucun filtre dropdown visible. - Schéma Prisma (prisma/schema.prisma) : LocationPret n'a pas de champ fournisseurTel (seulement fournisseurNom). - LocationForm.tsx ligne 78-84 : input pour fournisseurNom seulement, pas de champ tel. - /
- Correction validee : 1) Ajouter champ fournisseurTel String? au modèle LocationPret (prisma/schema.prisma). 2) Ajouter input tel dans LocationForm.tsx après ligne 84 : Field label="Tel fournisseur" avec Input type="tel". 3) Afficher fournisseur cliquable en ligne 106 : remplacer {l.fournisseurNom} par un lien &lt;a href={`tel:${l.fournisseurTel}`}&gt;{l.fournisseurNom}&lt;/a&gt; si fournisseurTel existe. 4) Ajouter fi

## 18. [P1-terrain] Workflow d'approbation scindé : approuver depuis la fiche demande ≠ approuver depuis la messagerie, pas d'une-action-rapide
- Source : parcours conducteur ; fichier : src/app/(app)/demandes/[id]/page.tsx + src/app/(app)/messagerie/ChantierFeed.tsx
- Scenario : Scenario 1 : notification « Demande de matériel créée ». Conducteur tape sur le lien, va sur /demandes/ID. Voit que c'est URGENT. Appelle l'admin, discute. Revient à la liste. Admin approuve depuis /demandes/ID en 2 clics (rare en pratique). Scenario 2 : message dans le fil chantier « Demande : ciment 20 sacs ». Conducteur peut cliquer sur « Approuver » (quickApproveDemandeToCommande) depuis le message. Commande créée auto. Plus rapide mais pas de note possible + UI cachée.
- Correction proposee : 1) Harmoniser : depuis la page demande, proposer aussi le bouton « Approuver → Créer commande » côté à côté de « Approuver » classique. 2) Les deux chemins (messagerie + fiche) doivent offrir les mêmes actions. 3) Depuis notifications, linker directement à la messagerie du chantier (le message de la demande) plutôt qu'à /demandes/ID, car c'est le flux plus naturel pour approuver.
- Statut : CONFIRME
- Contre-expertise : Fichier C:/Projects/outil-gestion-chantier/src/app/(app)/demandes/[id]/page.tsx ligne 217-282 : le formulaire d'approbation n'offre qu'un bouton "Approuver" simple (ligne 231-233) qui met la demande en APPROUVEE. L'admin doit ensuite cliquer sur "Créer la commande" (ligne 276-278) = 2 actions.   VS fichier C:/Projects/outil-gestion-chantier/src/app/(app)/messagerie/ChantierFeed.tsx ligne 888-914 :
- Correction validee : Sur la fiche demande /demandes/[id]/page.tsx, ajouter un second bouton de validation "Approuver et commander" (ou combiner les 2 actions dans le formulaire d'approbation) qui utilise la même logique que quickApproveDemandeToCommande. Le bouton simple "Approuver" doit rester pour les cas où l'admin approuve sans créer la commande immédiatement. Alternativement, changer le workflow pour que depuis l

## 19. [P1-terrain] Pas de numéro de contact fournisseur visible sur la fiche commande, contact impossibe en 1 clic
- Source : parcours conducteur ; fichier : src/app/(app)/commandes/[id]/page.tsx
- Scenario : Commande urgente en cours. Livreur appelle : « Je suis perdu ». Conducteur ouvre /commandes/ID, voit le fournisseur (« Point P »). Pas de tel. Doit quitter l'app, chercher le contact, revenir. 2-3 min perdues dans un moment critique.
- Correction proposee : 1) Ajouter champ fournisseurTel dans Commande. 2) Lors de la création, pré-remplir depuis un carnet (contacts fournisseurs), ou le saisir en form. 3) Afficher comme lien cliquable tel: + bouton « Appeler ».
- Statut : CONFIRME
- Contre-expertise : src/app/(app)/commandes/[id]/page.tsx:43 affiche le fournisseur en texte brut sans lien tel: ou numéro. prisma/schema.prisma:472-496 confirme que le model Commande n'a pas de champ fournisseurTel. CommandeForm.tsx:114-121 permet seulement de saisir le nom du fournisseur, pas un numéro. Aucun lien tel: cliquable pour appeler le fournisseur depuis la fiche commande.
- Correction validee : 1) Ajouter le champ `fournisseurTel String?` au model Commande dans prisma/schema.prisma. 2) Ajouter une migration `npx prisma migrate dev --name add_fournisseur_tel_to_commande`. 3) Dans CommandeForm.tsx (ligne 114-121), ajouter un Field pour `fournisseurTel` à côté du fournisseur, avec placeholder "+33 1 2X XX XX XX". 4) Dans la page detail /commandes/[id]/page.tsx (ligne 43), remplacer le texte

## 20. [P1-terrain] Bouton suppression photo invisible au toucher — hover-only
- Source : parcours navigation ; fichier : src/app/(app)/chantiers/[id]/pv-reception/ReserveList.tsx
- Scenario : Utilisateur terrain (smartphone) cherche à retirer une photo d'une réserve. Le bouton de suppression (p-0.5, 2.5px padding) est invisible par défaut avec opacity-0 group-hover:opacity-100, n'apparaît qu'au survol souris.
- Correction proposee : Remplacer opacity-0 group-hover:opacity-100 par opacity-100. Utiliser plutôt focus:opacity-100 pour indiquer le state au clavier. Augmenter le padding à p-1.5 minimum (24px).
- Statut : CONFIRME
- Contre-expertise : C:/Projects/outil-gestion-chantier/src/app/(app)/chantiers/[id]/pv-reception/ReserveList.tsx:164 contient `opacity-0 group-hover:opacity-100` qui rend le bouton de suppression de photo invisible au toucher sur mobile. Le bouton n'apparaît que lors du survol souris, ce qui n'existe pas sur écran tactile.
- Correction validee : ReserveList.tsx:164 — Remplacer className="absolute top-0.5 right-0.5 bg-black/60 text-white rounded-full p-0.5 opacity-0 group-hover:opacity-100 transition" par className="absolute top-0.5 right-0.5 bg-black/60 text-white rounded-full p-1.5 opacity-100 transition". Augmenter le padding de p-0.5 (2px) à p-1.5 (~6px) pour atteindre ~24px de cible tactile.

## 21. [P1-terrain] Bouton suppression photo invisible au toucher — hover-only
- Source : parcours navigation ; fichier : src/app/(app)/chantiers/[id]/pv-reception/ReserveForm.tsx
- Scenario : Utilisateur terrain supprime une photo depuis le formulaire d'édition d'une réserve. Le bouton X (p-0.5, opacity-0 group-hover) n'apparaît qu'au survol souris, invisible au doigt.
- Correction proposee : Remplacer opacity-0 group-hover:opacity-100 par opacity-100 permanent. Augmenter p-0.5 à p-1.5 minimum pour 24px de cible tactile.
- Statut : CONFIRME
- Contre-expertise : src/app/(app)/chantiers/[id]/pv-reception/ReserveForm.tsx:236 — Le bouton de suppression de photo a `opacity-0 group-hover:opacity-100 focus:opacity-100` et `p-0.5` (2px padding). Sur mobile/tactile, le groupe hover-only ne s'active jamais et le padding reste insuffisant pour une cible tactile (< 24px). Le focus:opacity-100 ajouté aide au clavier mais ne résout pas le toucher.
- Correction validee : Remplacer ligne 236 : `className="absolute top-0.5 right-0.5 bg-black/60 text-white rounded-full p-0.5 opacity-0 group-hover:opacity-100 focus:opacity-100 transition"` par : `className="absolute top-0.5 right-0.5 bg-black/60 text-white rounded-full p-1.5 opacity-100 transition"`  Cela rend le bouton toujours visible (opacity-100 permanent) et augmente le padding à p-1.5 (6px) pour une cible d'envi

## 22. [P1-terrain] Actions tâches invisibles au toucher (renommer section, cycle priorité, ajouter sous-tâche)
- Source : parcours navigation ; fichier : src/app/(app)/planning/TacheListTodoist.tsx
- Scenario : Utilisateur mobile édite le planning. Boutons « Renommer » section, « Cycle priorité » et « + Sous-tâche » avec className='p-1 opacity-0 group-hover:opacity-100' sont invisibles au doigt, apparaissent uniquement au survol souris.
- Correction proposee : Remplacer opacity-0 group-hover:opacity-100 par opacity-100 permanent. Augmenter p-1 à p-1.5 ou p-2 pour atteindre 40px minimum de cible tactile (padding 6-8px). Sinon, placer ces boutons visiblement dans le flux normal, pas en hover.
- Statut : CONFIRME
- Contre-expertise : src/app/(app)/planning/TacheListTodoist.tsx:421 et 752 — Boutons d'actions (Renommer, Cycle priorité, Ajouter sous-tâche) ont `className="...opacity-0 group-hover:opacity-100..."` sans alternative tactile. Le `hover:opacity-100` (ligne 421) aide au survol souris mais n'existe qu'une seule fois. Le `focus-within:opacity-100` aide au clavier. MAIS : sur un écran tactile (mobile), aucun hover n'exist
- Correction validee : Remplacer `opacity-0 group-hover:opacity-100` par `[@media(hover:hover)]:opacity-0 [@media(hover:hover)]:group-hover:opacity-100` pour que sur tactile, les boutons restent visibles (opacity-100 par défaut). Ou alternative : remplacer par `opacity-100` direct pour toujours visibles. Augmenter padding `p-1` à `p-1.5` ou `p-2` pour cible de 40px minimum (fichiers concernés : ligne 421-444 SectionHead

## 23. [P1-terrain] Boutons action tâche avec padding trop petit (p-1 = 4px)
- Source : parcours navigation ; fichier : src/app/(app)/planning/TacheListTodoist.tsx, src/app/(app)/planning/page.tsx
- Scenario : Utilisateur mobile modifie une tâche dans le planning. Boutons « Cycle priorité » (p-1) et « Modifier » sont avec p-1 rounded (4px padding, ~20px cible), sous minimum 40px pour écran tactile 375px.
- Correction proposee : Augmenter padding de p-1 à p-2 (8px, ~32px) minimum. Idéalement p-2.5 ou p-3 pour 40px. Alternativement, utiliser lg:p-1 + base p-2 pour responsive.
- Statut : CONFIRME
- Contre-expertise : Fichier: C:/Projects/outil-gestion-chantier/src/app/(app)/planning/TacheListTodoist.tsx, lignes 753-794. Tous les boutons d'action (cycle priorité ligne 759, ajouter sous-tâche ligne 769, modifier ligne 780, supprimer ligne 791) utilisent `p-1` (4px padding) avec des icônes 14px, créant une cible tactile de ~20px, bien sous le minimum WCAG 44px pour écrans tactiles mobile (375px).
- Correction validee : Augmenter le padding des boutons d'action de la TacheRow (lignes 759, 769, 780, 791) de p-1 à p-2.5 (10px) ou au minimum p-2 (8px). Exemple ligne 759: remplacer `className="p-1 rounded..."` par `className="p-2.5 rounded..."` pour tous les 4 boutons de la section Actions à droite.

## 24. [P2-navigation] changeUserRole() ne nettoie pas les membresCanaux après changement de rôle
- Source : parcours admin ; fichier : src/app/(app)/admin/users/actions.ts
- Scenario : Admin change le rôle d'un utilisateur de CHEF à CLIENT (ou CHEF à ADMIN). Les ChantierMembre et CanalMembre existants ne sont pas supprimés : l'utilisateur reste 'de droit' membre de chantiers où il n'a plus accès (CLIENT ne doit pas voir les chantiers des CHEF).
- Correction proposee : Dans changeUserRole(), après db.user.update(), nettoyer les ChantierMembre et CanalMembre si le nouveau rôle l'exige (ex: CLIENT → garder que ses chantiers explicites). Ou décrire la stratégie en docstring.
- Statut : CONFIRME
- Contre-expertise : src/app/(app)/admin/users/actions.ts:97-123 - La fonction changeUserRole() ne supprime pas les ChantierMembre ou CanalMembre après changement de rôle. Ces tables contrôlent l'accès (requireChantierAccess() ligne 179-182 de auth-helpers.ts utilise ChantierMembre; listChannelsFor() ligne 42 de channel-queries.ts utilise CanalMembre). Un utilisateur changé de CHEF→CLIENT conserve ses ChantierMembre e
- Correction validee : Dans changeUserRole() (actions.ts:97-123), ajouter après l'update du rôle utilisateur (ligne 109-112) : db.$transaction([db.chantierMembre.deleteMany({where:{userId}}), db.canalMembre.deleteMany({where:{userId}})]) pour nettoyer tous les memberships. Ou, mieux : clarifier la stratégie en docstring (ex: expliciter si certains rôles conservent leurs memberships lors d'une transition, et documenter l

## 25. [P2-navigation] Formulaire 'Nouveau paiement' encombré sur téléphone — grille 3+ colonnes
- Source : parcours admin ; fichier : src/app/(app)/paie/nouveau/page.tsx
- Scenario : Sur téléphone (375px), le formulaire ligne 179 affiche : Ouvrier (5 col), Du (3 col), Au (3 col), Bouton (1 col) — soit 12 colonnes Tailwind. Sur mobile ça s'empile et les inputs deviennent trop étroits. Les presets de date occupent 6 rows à eux seuls.
- Correction proposee : Restructurer en stack mobile (1 colonne) jusqu'à sm: (tablet). Presets : afficher en wrap horizontal compact avec chip scrollable si besoin. Inputs dates : taille normale mais sur une ligne chacun.
- Statut : REJETE
- Contre-expertise : Le formulaire "Nouveau paiement" situé à C:/Projects/outil-gestion-chantier/src/app/(app)/paie/nouveau/page.tsx ligne 180 utilise déjà `grid-cols-1 sm:grid-cols-12`, ce qui crée une mise en page responsive correcte : sur mobile (< 640px), tous les champs s'empilent en 1 seule colonne (grid-cols-1), ce qui est conforme à la correction proposée dans le constat. Sur tablette+ (sm:), la grille passe à

## 26. [P2-navigation] Sélecteur de période : 6 chips + formulaire GET sur une même ligne — ne tient pas sur téléphone
- Source : parcours admin ; fichier : src/app/(app)/paie/page.tsx
- Scenario : Page paie ligne 376-488 : 6 chips presets (Aujourd'hui, Cette semaine, 7 jours, etc.) + formulaire 5 selects sur même ligne. Sur téléphone, tout s'empile ou s'écrase. L'user terrain doit scroller horizontalement pour voir tous les filtres.
- Correction proposee : Mobile : chips en 2-3 lignes (auto-wrap). Formulaire filtres en stack (1 colonne) jusqu'à sm:. Sur sm+ (tablet) passer à 2-3 colonnes. Les boutons Appliquer/Effacer restent en bas avec largeur 100% sur mobile.
- Statut : REJETE
- Contre-expertise : C:/Projects/outil-gestion-chantier/src/app/(app)/paie/page.tsx:378-491. Les chips presets utilisent `flex flex-wrap` (ligne 378) qui wrappent automatiquement. Le formulaire utilise `grid grid-cols-2 sm:grid-cols-12` (ligne 411) : sur mobile (< 640px), grille 2 colonnes qui stack verticalement sans forcer scroll horizontal. Depuis le commit fdcdc79 ("Paie : recherche + filtres"), le layout est resp

## 27. [P2-navigation] Paie complètement faisable sur téléphone mais mise en page désorganisée
- Source : parcours admin ; fichier : src/app/(app)/paie/page.tsx
- Scenario : Un administrateur terrain (sur téléphone) doit : consulter la paie du mois, filtrer par chantier, sélectionner 3-4 paiements, cliquer 'Payer 4', et valider. C'est possible mais le flux est pénible : trop de scrolls, filtres éparpillés, boutons et liens petits (sous 44px).
- Correction proposee : Restructurer /paie pour mobile : Header compact. Presets chips en auto-wrap. Filtres en modal/collapse plutôt qu'inline sur mobile. Paiements 'À verser' : checkbox + actions au clic sur la ligne. Stat cards : 2 colonnes max sur mobile.
- Statut : CONFIRME
- Contre-expertise : Fichier src/app/(app)/paie/page.tsx : Ligne 411 affiche le grid des filtres en `grid grid-cols-2 sm:grid-cols-12` (grille 2 colonnes sur mobile). Lignes 413-476 contiennent 6 filtres (Du, Au, Recherche, Contrat, Chantier, Mode) inline et éparpillés. Lignes 378-405 : les chips presets ont `py-1` (~28-32px hauteur, sous 44px cible tactile). Inputs ont `py-2` (~32-36px, aussi sous 44px). Le code CONT
- Correction validee : Restructurer les filtres sur mobile (< 640px) : convertir le form grid en collapse/accordion ou drawer mobile, garder seulement presets chips visibles. Alternative rapide : ajouter un bouton "Filtres" cachant/affichant le form sous `sm:` breakpoint. Augmenter hauteur inputs/chips à 44px minimum (py-3 + text-base). Limiter stat cards à max-width sur mobile si besoin.

## 28. [P2-navigation] Sorties matériel : la validation 'équipe OU chantier' n'est pas appliquée côté client
- Source : parcours chef ; fichier : src/app/(app)/sorties/nouvelle/page.tsx
- Scenario : BEN sort du matériel, laisse vide BOTH équipe ET chantier (les deux sont 'optionnel' au visuel du form). Il clique 'Enregistrer' → erreur serveur vague. DEFAUT : pas de feedback immédiat sur mobile, il doit relire le texte 'Au moins équipe ou chantier' tout petit en haut du formulaire.
- Correction proposee : Ajouter validation côté client (form.checkValidity() ou Zod) qui montre une alerte claire AVANT submit. Ou rendre l'un des deux REQUIS et masquer l'autre (radio ou logic).
- Statut : CONFIRME
- Contre-expertise : C:/Projects/outil-gestion-chantier/src/app/(app)/sorties/nouvelle/page.tsx:57-77 — les deux selects equipeId et chantierId n'ont pas l'attribut required et aucune validation JavaScript côté client. C:/Projects/outil-gestion-chantier/src/app/(app)/sorties/actions.ts:26-28 — validation métier serveur exists mais throw une erreur qui remonte mal au client sans toast/feedback UX clair. Scénario: utili
- Correction validee : Ajouter validation côté client au formulaire sorties/nouvelle/page.tsx: 1) Ajouter required aux deux selects ET ajouter custom HTML5 validation avec JavaScript, OU 2) Créer un composant form wrapper avec useActionState() pour capturer l'erreur et afficher un toast/alerte claire avant de rejeter le submit. Exemple: onSubmit handler qui vérifie if (!equipeId && !chantierId) { alert(...); return; }. 

## 29. [P2-navigation] Incident : chantier NON verrouillé en édition (risque de confusion)
- Source : parcours chef ; fichier : src/app/(app)/incidents/[id]/page.tsx + IncidentForm.tsx
- Scenario : BEN signale un incident depuis le chantier A. Plus tard, on lui montre la liste, il ouvre l'incident pour l'éditer. Il NE VOIT PAS que c'est pour le chantier A en grand. Le champ chantier reste mutable. Il pourrait accidentellement changer de chantier. DEFAUT : le champ n'est pas disabled et aucun indice visuel du chantier courant.
- Correction proposee : En édition d'incident, verrouiller le chantier (disabled) et afficher en fort le nom du chantier en haut. Ou mieux : garder le chantier dans l'URL et ne pas l'exposer au formulaire (comme la demande fait : chantierId hidden).
- Statut : CONFIRME
- Contre-expertise : C:/Projects/outil-gestion-chantier/src/app/(app)/incidents/IncidentForm.tsx:118-129 — Le champ Select pour le chantier n'a pas d'attribut disabled en mode édition (quand incident est présent). Le composant reçoit bien incident?.chantierId depuis /incidents/[id]/page.tsx:169-181, mais aucun verrouillage n'est appliqué. En C:/Projects/outil-gestion-chantier/src/app/(app)/incidents/[id]/page.tsx:106-
- Correction validee : Dans IncidentForm.tsx ligne 118, ajouter l'attribut disabled au Select : &lt;Select name="chantierId" defaultValue={incident?.chantierId ?? defaultChantierId ?? ""} disabled={!!incident}&gt; pour verrouiller le chantier en mode édition. Optionnel : afficher le nom du chantier en accent en haut de la page d'édition (page.tsx:68-86) avec un Badge ou section visuelle.

## 30. [P2-navigation] Incidents & rapports : bouton Retour en haut, MAIS pas de bouton 'Annuler' au pied du formulaire
- Source : parcours chef ; fichier : src/app/(app)/incidents/nouveau/page.tsx, /incidents/[id]/page.tsx
- Scenario : BEN remplit un formulaire sur mobile, se trompe à mi-chemin, veut revenir. Il cherche un bouton 'Annuler' en bas du formulaire (UX pattern attendu). Il NE TROUVE QUE 'Enregistrer'. Il doit scroller tout en haut pour cliquer 'Retour'. DEFAUT : friction élevée pour un abandon.
- Correction proposee : Ajouter bouton 'Annuler' en bas à gauche dans tous les formulaires (incidents, rapports). Ou : le chevron retour en haut-gauche doit être SUPER visible et accessible au pouce (en mobile, haut-gauche = loin du pouce).
- Statut : CONFIRME
- Contre-expertise : IncidentForm.tsx:205-209 montre que le bouton "Annuler" ne s'affiche que si onCancel est fourni (ligne 205: {onCancel &&}). Or dans incidents/nouveau/page.tsx:25-29, IncidentForm est appelé SANS onCancel. De même pour la page edit incidents/[id]/page.tsx:169-181. Idem pour RapportForm dans rapports/nouveau/page.tsx:81 qui n'a pas onCancel (le bouton n'apparaît pas), tandis que RapportsSection.tsx:
- Correction validee : Dans incidents/nouveau/page.tsx et incidents/[id]/page.tsx, passer onCancel={() => router.back()} à IncidentForm. Dans rapports/nouveau/page.tsx, passer onCancel={() => router.back()} à RapportForm. Le composant est déjà prêt (logique existe), il suffit de l'alimenter depuis les pages parent.

## 31. [P2-navigation] Rapport : champ Date requis mais impossible d'accéder sur ANDROID ancien (date picker limité)
- Source : parcours chef ; fichier : src/app/(app)/rapports/RapportForm.tsx (ligne 138-145)
- Scenario : BEN sur Android 8 ou plus ancien, input type=date affiche un calendrier basique. Il veut mettre une date passée (hier = il rentre de chantier et poste son rapport). Sur certains appareils, le clavier date refuse les dates < aujourd'hui (ou l'UI du date picker bugue). DEFAUT : pas de fallback texte, il est bloqué.
- Correction proposee : Ajouter un fallback avec une select/combo en YYYY-MM-DD ou utiliser une libraire de date picker (react-datepicker) avec validation côté client. Ou au minimum : supprimer max={today} et laisser l'utilisateur corriger après.
- Statut : CONFIRME
- Contre-expertise : src/app/(app)/rapports/RapportForm.tsx:143 — Input type="date" avec max={today} empêche effectivement la saisie de dates passées. Le scénario utilisateur est plausible : un chef de chantier qui rentre le soir et poste son rapport du jour même (hier si posté tard) est bloqué par cette restriction. Aucun fallback n'est implémenté, aucune librairie alternative ne remplace le date picker natif.
- Correction validee : Retirer l'attribut max={today} de la ligne 143, ou implémenter un fallback avec react-datepicker/autre librairie pour Android ancien. Minimum : supprimer max et laisser la validation métier côté serveur.

## 32. [P2-navigation] Fiche chantier : pas de bouton Retour vers la liste (backHref)
- Source : parcours chef ; fichier : src/app/(app)/chantiers/[id]/page.tsx (pas de bouton Retour dans PageHeader)
- Scenario : BEN ouvre un chantier depuis le dashboard, consulte les détails, puis veut revenir à la liste. Il scrolle tout en haut du plateau pour chercher un bouton retour. Il N'EN TROUVE PAS. Il doit utiliser le back du browser (Android) ou deviner qu'il faut cliquer sur 'Chantiers' dans la sidebar (si elle est visible). DEFAUT : UX attendu non respecté.
- Correction proposee : Ajouter backHref='/chantiers' dans le PageHeader de la fiche chantier.
- Statut : REJETE
- Contre-expertise : C:/Projects/outil-gestion-chantier/src/app/(app)/chantiers/[id]/page.tsx:133 contient déjà backHref="/chantiers" dans le PageHeader. C:/Projects/outil-gestion-chantier/src/components/ui/PageHeader.tsx:18-26 affiche correctement un lien "Retour" avec icône ChevronLeft quand backHref est fourni. Le constat prétend que le bouton Retour manque, mais le code contient déjà la correction proposée.

## 33. [P2-navigation] Main : pb-24 padding-bottom OK pour la nav mobile, MAIS formulaires longs se retrouvent JAMAIS visibles
- Source : parcours chef ; fichier : src/app/(app)/layout.tsx (ligne 175, pb-24)
- Scenario : BEN sur un rapport avec 8 photos à ajouter, il scrolle down. Le formulaire a pb-24 = 96px de padding en bas pour la nav mobile. Mais sur un formulaire TRES long (comme rapport avec photos), le bouton 'Publier' est hors écran AUSSI avec le clavier ouvert. DEFAUT : ne peut pas soumetre sans fermer le clavier manuellement.
- Correction proposee : Utiliser pb-24 seulement sur LISTES (incidents, demandes, sorties), PAS sur formulaires. Ou utiliser un sticky footer qui remonte AVEC le clavier. Ou réduire pb en focus (CSS :focus-within).
- Statut : CONFIRME
- Contre-expertise : src/app/(app)/layout.tsx:175 a `<main className="flex-1 pb-24 md:pb-0">` appliqué à TOUTES les pages. RapportForm.tsx (lignes 128-289) n'a pas de scroll-into-view ni sticky footer. Sur mobile avec clavier ouvert + formulaire long (8 photos), le bouton "Publier" ligne 284 peut être hors écran. pb-24 = 96px padding crée de l'espace scrollable inutile sous le formulaire. Scénario plausible : BEN avec
- Correction validee : Appliquer pb-24 SEULEMENT aux pages de listes (incidents, demandes, sorties). Alternativement, ajouter `pb-0` en classe conditionnelle sur les pages formulaire (rapports/nouveau, rapports/[id], incidents/nouveau, etc.). Ou : utiliser un sticky footer avec `sticky bottom-0 pt-2 border-t` sur le bouton du formulaire pour qu'il reste visible même avec clavier ouvert.

## 34. [P2-navigation] Menu mobile client affiche 'Rapports quotidiens' accessible mais sans lien retour vers dashboard
- Source : parcours client ; fichier : src/components/NavSidebar.tsx (lignes 161-166)
- Scenario : Utilisateur CLIENT sur mobile : je clique sur 'Rapports' depuis le menu primaire (onglet 4/5), puis j'ouvre un rapport. Pas de bouton retour clair — je dois swiper ou chercher le bouton de la barre. Le label 'Rapports quotidiens' est jargon pour non-professionnel (pourquoi 'quotidiens' ?)
- Correction proposee : Ajouter backHref explicite dans PageHeader + clarifier le label 'Rapports quotidiens' → 'Avancement du chantier' ou 'Notes du terrain'. Sur mobile, assurer que tout écran a un bouton retour visuel (utiliser chevron/arrow en haut à gauche).
- Statut : CONFIRME
- Contre-expertise : Fichier C:/Projects/outil-gestion-chantier/src/app/(app)/rapports/page.tsx, lignes 80-94 : PageHeader n'a pas de backHref. Le composant PageHeader (C:/Projects/outil-gestion-chantier/src/components/ui/PageHeader.tsx, lignes 18-26) affiche un bouton "Retour" uniquement si backHref est fourni. Actuellement, absent → pas de lien retour visuel pour le CLIENT. Scenario utilisateur valide : CLIENT sur m
- Correction validee : Ajouter backHref="/dashboard" au PageHeader de src/app/(app)/rapports/page.tsx ligne 80. Exemple : <PageHeader title="Rapports de chantier" description="..." backHref="/dashboard" action={...} />

## 35. [P2-navigation] 'Rapports de chantier' → vocabulaire confus : c'est les rapports quotidiens des CHEFS, pas du CLIENT
- Source : parcours client ; fichier : src/app/(app)/rapports/page.tsx (ligne 82-93)
- Scenario : CLIENT lit 'Rapports de chantier' dans le titre + description 'Compte-rendus journaliers postés par les chefs' — il peut penser que c'est SES rapports à écrire, ou que c'est un document officiel. Sur un chantier de bâtiment, un CLIENT (maître d'ouvrage) confond avec 'rapport technique' ou PV de réception.
- Correction proposee : Titre → 'Avancement journalier' ou 'Notes du chantier' ; description → 'Mises à jour quotidiennes de l'équipe sur l'avancement' (langage CLIENT, pas jargon 'compte-rendus').
- Statut : CONFIRME
- Contre-expertise : Code: src/app/(app)/rapports/page.tsx lignes 81-82 affiche "Rapports de chantier" + "Compte-rendus journaliers postés par les chefs". CLIENT peut accéder (line 45: showJournal check). Vocabulaire confus pour maître d'ouvrage qui attendrait documents techniques/PV, pas notes d'avancement.
- Correction validee : Ligne 81-82 : Remplacer title: "Rapports de chantier" par "Avancement journalier" ou "Notes du chantier" ; description: "Compte-rendus journaliers postés par les chefs" par "Mises à jour quotidiennes de l'équipe sur l'avancement"

## 36. [P2-navigation] CLIENT voit lien 'Voir demande' et 'Voir commande' qui leadent vers pages inaccessibles (403/redirect)
- Source : parcours client ; fichier : src/app/(app)/rapports-hebdo/HebdoMessageRow.tsx (lignes 242-257)
- Scenario : CLIENT regarde un rapport hebdo. Admin a inclus un message système SYSTEM_DEMANDE ou SYSTEM_COMMANDE (ligne 51). Le lien 'Voir demande' (ligne 244) ou 'Voir commande' (ligne 250) sont cachés pour CLIENT (!isClient, ligne 242 & 250). Mais si message TEXT mentionne manuelle une demande ou commande, CLIENT clique et reçoit impasse (redirect vers dashboard sans message d'erreur).
- Correction proposee : Vérifier que les liens dans les textes de messages (m.texte) ne contiennent pas d'URLs ou de références à /demandes/* ou /commandes/*. Ou : appliquer un filtre de sanitization qui remplace les URLs internes par du texte. Actuellement : texte brut accepté — risque de social engineering ou lien cassé.
- Statut : REJETE
- Contre-expertise : Les liens cliquables "Voir demande" et "Voir commande" sont DEJA cachés pour CLIENT à HebdoMessageRow.tsx:242 et :250 (condition !isClient). Le texte brut du message (m.texte) n'est PAS linkifié automatiquement — c'est juste du texte affiché en whitespace-pre-wrap. Un CLIENT ne peut pas "cliquer" sur du texte brut. Si un CLIENT accède manuellement à /demandes/* ou /commandes/*, l'auth.config.ts:40

## 37. [P2-navigation] Redirection silencieuse si CLIENT essaie d'accéder au journal quand showJournal=false
- Source : parcours client ; fichier : src/app/(app)/chantiers/[id]/journal/page.tsx (lignes 42-44)
- Scenario : ADMIN désactive showJournal pour un CLIENT. CLIENT clique sur lien 'Journal' depuis le chantier. Le code redirige vers /chantiers/{id} sans toast ou message. CLIENT voit juste 'rien ne se passe'. Non intuitif sur mobile (swiper ou retour APP vs page refresh).
- Correction proposee : Afficher un toast ou message doux : 'L'accès au journal vous est actuellement restreint' AVANT la redirection, ou pré-vérifier en client-side (JS) avec un lien désactivé + tooltip. Ou utiliser une carte 'Accès restreint' au lieu de redirection silencieuse.
- Statut : CONFIRME
- Contre-expertise : Le code source confirme le défaut :  1. Redirection silencieuse : src/app/(app)/chantiers/[id]/journal/page.tsx:42-44    ```typescript    if (me.isClient && !me.visibility.showJournal) {      redirect(`/chantiers/${id}`);    }    ```  2. Le CLIENT voit les liens journal sur le dashboard : src/app/(app)/dashboard/ClientDashboard.tsx:80-90 charge journauxRecents SANS filtrer par showJournal.  3. Les
- Correction validee : Afficher un toast ou message dans journal/page.tsx AVANT la redirection, ou filtrer les journauxRecents par me.visibility.showJournal en ClientDashboard.tsx. Exemple de correction simple (journal/page.tsx):  Avant la redirection (ligne 42-44), ajouter : ```typescript if (me.isClient && !me.visibility.showJournal) {   const { cookies } = await import('next/headers');   // Afficher un toast via cook

## 38. [P2-navigation] Accès CLIENT restrictif mais '/rapports' accessible sans filtre showJournal au niveau auth
- Source : parcours client ; fichier : src/auth.config.ts (lignes 40-54)
- Scenario : ADMIN peut désactiver showJournal pour CLIENT. CLIENT essaie /rapports → auth gate laisse passer (il est CLIENT, /rapports est dans clientAllowedRoots). Puis rapports/page.tsx fait redirect si !showJournal (ligne 45). C'est deux vérifications : une au middleware (auth.config.ts), une au component. Asymétrique. Risk : si quelqu'un oublie la deuxième vérification.
- Correction proposee : Documenter clairement que visibility checks (showJournal, showIncidents, etc.) se font au COMPONENT level, pas au middleware. Ou : porter tous les visibility checks au middleware (plus coûteux, mais plus sûr). Actuellement : cohérent mais non-obvious.
- Statut : CONFIRME
- Contre-expertise :  Le constat est exact. Preuve de l'asymétrie:  1. auth.config.ts:40-54: CLIENT peut accéder à /rapports, /incidents, /chantiers (clientAllowedRoots)    - Aucune vérification de showJournal, showIncidents, showPlans au middleware  2. src/app/(app)/rapports/page.tsx:45-48:     - Fait un check CLIENT-only: `if (me.isClient && !me.visibility.showJournal) { redirect("/dashboard") }`  3. src/app/(app)/i
- Correction validee :  Documenter clairement que les visibility checks (showJournal, showIncidents, showPlans) se font AU COMPONENT LEVEL, jamais au middleware. Ajouter un commentaire dans auth.config.ts ligne 40 expliquant que /rapports, /incidents, /chantiers laissent passer CLIENT en confiance des vérifications locales. OU: à titre de sécurité en profondeur, enrichir le middleware pour porter aussi les visibility ch

## 39. [P2-navigation] Planning charge mal sur mobile, sélecteur de chantier occupe trop d'espace, pas de vue rapide des retards
- Source : parcours conducteur ; fichier : src/app/(app)/planning/page.tsx
- Scenario : Conducteur ouvre Planning sur mobile (375px). Les 5 boutons de vue (Gantt/Kanban/Calendrier/PERT/Liste) + select chantier + bouton reset prennent 2 lignes. Les commandes en retard et locations en retard (affichées via l'overlay « events ») ne sont pas immédiatement visibles - il faut cliquer sur une vue pour les voir.
- Correction proposee : 1) Mobile-first : réduire les boutons de vue à des icônes (16px) sans texte, utiliser un drawer pour les noms. 2) Placer le sélecteur de chantier et reset sur une ligne dédiée. 3) Au-dessus du planning, afficher un compact « N retards : M commandes + P locations » cliquable.
- Statut : CONFIRME
- Contre-expertise : Le code montre deux défauts avérés :  1. **Boutons de vue occupent beaucoup d'espace** : src/app/(app)/planning/page.tsx:186-247 affiche 5 boutons (Gantt, Kanban, Calendrier, PERT, Liste) avec texte complet en `px-3 py-2 text-sm` sans classe responsive. Aucune réduction à icônes sur mobile.  2. **Pas de vue rapide des retards** :     - Aucun compteur des retards n'est affiché en haut du planning (
- Correction validee : 1. Remplacer les 5 boutons de vue par des icônes sur mobile (ex: lucide-react Calendar, Grid3x3, GitBranch, ListTodo) avec `sm:text-label sm:flex-row` pour afficher le texte sur tablet+. 2. Ajouter un compteur des retards AVANT/APRÈS la sélection du chantier : calculer const retardCount = events.filter(e => e.date < new Date()).length; afficher `<div className="text-sm font-semibold">N retards</di

## 40. [P2-navigation] Bouton « Envoyer au client » demande confirmation, rapport envoyé sans pouvoir vérifier les filtres appliqués juste avant
- Source : parcours conducteur ; fichier : src/app/(app)/chantiers/[id]/rapport-hebdo/page.tsx
- Scenario : Vendredi 17h. Conducteur compile le rapport hebdo. Admin masque 5 messages sensibles (retards d'équipes). Clique « Envoyer au client ». Pas de vraie confirmation ni double-check du contenu visible. Client reçoit le rapport 30 sec plus tard. Si une erreur (message sensible non masqué), impossible de rappeler.
- Correction proposee : 1) Ajouter une modale de confirmation avant envoi : « Vous allez envoyer le rapport au client. Vérifiez le contenu cidessous. » 2) Afficher un aperçu du rapport EXACT (texte de l'intro + liste des messages visibles). 3) Bouton « Annuler » et « Confirmer l'envoi ». 4) Ajouter un undo « Rappeler le rapport » qui demande une note de justification (admin → client).
- Statut : CONFIRME
- Contre-expertise : Fichier: C:/Projects/outil-gestion-chantier/src/app/(app)/chantiers/[id]/rapport-hebdo/page.tsx, lignes 341-349. Le bouton « Envoyer au client » est un simple formulaire sans modale de confirmation. Aucune modale de vérification du contenu n'apparaît avant l'envoi. L'encart informatif (lignes 291-319) affiche les compteurs de messages filtrés, mais ne constitue pas une véritable modale de confirma
- Correction validee : Créer un composant DialogConfirm réutilisable, puis ajouter en client-side une modale qui s'affiche au clic sur « Envoyer au client ». La modale doit: 1) afficher un aperçu du rapport (intro + liste des N messages visibles au client) avec compteur rouge si messages cachés, 2) proposer boutons « Annuler » et « Confirmer l'envoi », 3) envoyer le formulaire seulement après confirmation. Alternativeme

## 41. [P2-navigation] PV réserves : pas de tri par deadline des réserves, pas de visibilité sur les retards
- Source : parcours conducteur ; fichier : src/app/(app)/chantiers/[id]/pv-reception/page.tsx
- Scenario : PV de réception avec 12 réserves. Certaines ont une dateLimite, d'autres non (null). Page affiche liste en ordre createdAt. Conducteur ne voit pas immédiatement laquelle est en retard. Doit ouvrir chacune pour vérifier la date.
- Correction proposee : 1) Trier ReserveList par (dateLimite DESC, createdAt DESC) afin que les retards (leveLe === null && dateLimite < now) remontent d'abord. 2) Afficher un petit indicateur rouge « En retard - X jours » si applicable. 3) Ajouter un compteur en en-tête « N réserves en retard ».
- Statut : CONFIRME
- Contre-expertise : C:/Projects/outil-gestion-chantier/src/app/(app)/chantiers/[id]/pv-reception/page.tsx:78 (et ligne 91) — les réserves sont triées par `numero: "asc"` uniquement, pas par dateLimite. PvWorkspace.tsx:219 montre le comptage de nbOuvertes/nbLevees mais pas de nbEnRetard. Le tri par délai n'est pas implémenté — les réserves en retard ne remontent pas automatiquement en haut de la liste.
- Correction validee : Modifier page.tsx ligne 78 et 91 : remplacer `orderBy: { numero: "asc" }` par `orderBy: [{ leveLe: "asc" }, { dateLimite: { sort: "desc", nulls: "last" } }, { numero: "asc" }]` pour que les réserves non levées s'affichent d'abord, puis triées par dateLimite (retards d'abord). Ajouter un compteur nbEnRetard dans PvWorkspace.tsx ligne 218 : `const nbEnRetard = reserves.filter((r) => !r.leveLe && r.d

## 42. [P2-navigation] Demandes matériel dans le fil : boutons d'action (Approuver/Refuser) n'apparaissent pas sur mobile, ou trop petits, pas évident
- Source : parcours conducteur ; fichier : src/app/(app)/messagerie/[chantierId]/page.tsx + ChantierFeed.tsx
- Scenario : Demande affichée dans le fil chantier. Sur desktop, les boutons Check/X sont à côté du texte. Sur mobile (375px), les boutons sont trop serres ou masqués (horizontal scroll). Conducteur ne voit pas qu'il peut approuver depuis le fil - va chercher la demande ailleurs.
- Correction proposee : 1) Sur mobile, afficher les boutons approuver/refuser EN DESSOUS du texte de la demande, sur une ligne dédiée (full width, 44px tall minimum pour les doigts). 2) Utiliser des libellés clairs : « Approuver + Commande » / « Refuser ». 3) Icône + texte au lieu de juste icône.
- Statut : CONFIRME
- Contre-expertise : Le code de ChantierFeed.tsx (lignes 894-914) affiche les boutons Approuver/Refuser pour les demandes matériel avec : - padding vertical py-1 (4px) — sous le minimum tactile WCAG de 44px - padding horizontal px-2 (8px) — très insuffisant - text-xs — taille réduite - affichage côte à côte (flex-wrap) sans classe responsive mobile - pas de full width ni de réorganisation pour écrans tactiles  En comp
- Correction validee : Remplacer le bloc des boutons (lignes 894-914 de ChantierFeed.tsx) par une version responsive mobile-first : - Sur mobile : afficher les boutons en stack vertical (flex-col) avec w-full et py-2.5 (10px) ou py-3 (12px) minimum - Augmenter le padding vertical global à py-2 minimum (8px) - Ajouter des classes md:flex-row md:gap-2 pour retrouver l'affichage côte à côte sur desktop - Garder le texte cl

## 43. [P2-navigation] Liste des chantiers : pas de distinction visuelle claire des chantiers en retard/alerte
- Source : parcours conducteur ; fichier : src/app/(app)/chantiers/page.tsx
- Scenario : Conducteur ouvre l'accueil, voit 3 chantiers. L'un a 4 réserves en retard, l'autre 2 locations en retard. Aucun badge d'alerte sur le chantier. Doit cliquer sur chacun pour découvrir les problèmes.
- Correction proposee : 1) Pour chaque chantier, calculer un compteur d'alertes : retards(locations) + retards(commandes) + réserves non levées. 2) Afficher un petit badge rouge avec le chiffre (ex «2 🔴 ») si > 0. 3) Trier les chantiers : alertes DESC, statut ASC.
- Statut : CONFIRME
- Contre-expertise : Fichier: C:/Projects/outil-gestion-chantier/src/app/(app)/chantiers/page.tsx (lignes 30-52, 147-194). La requête db.chantier.findMany() ne charge pas les données de retards (locations en retard, commandes en retard, réserves non levées). L'affichage de chaque chantier (lignes 147-194) ne contient aucun badge d'alerte. Comparaison: dans locations/page.tsx:35, la logique enRetard existe (dateFinPrev
- Correction validee : 1) Dans chantiers/page.tsx, modifier la requête findMany() pour inclure des _count sur les locations/commandes en retard et les réserves non levées. 2) Calculer un compteur d'alertes côté serveur (locations en retard + commandes en retard + réserves non levées). 3) Afficher un badge rouge avec le nombre d'alertes si > 0 sur la carte du chantier (ex: "3 🔴"). 4) Trier les chantiers: alertes DESC, st

## 44. [P2-navigation] Pas de chemin de retour vers l'administration
- Source : parcours navigation ; fichier : src/app/(app)/admin/audit/page.tsx
- Scenario : Admin consulte le journal d'audit. La page n'a pas de PageHeader avec backHref. Aucun bouton « Retour » visible. Doit utiliser le bouton arrière du navigateur.
- Correction proposee : Ajouter backHref="/admin/users" ou backHref à PageHeader pour revenir à l'accueil admin. Cohérence : tous les sous-écrans admin doivent avoir un chemin de retour.
- Statut : CONFIRME
- Contre-expertise : src/app/(app)/admin/audit/page.tsx:78 — PageHeader appelé sans prop backHref. Le composant PageHeader (src/components/ui/PageHeader.tsx:18-26) supporte le backHref et affiche un lien retour avec chevron si fourni. L'admin consultant le journal d'audit doit utiliser le bouton arrière du navigateur pour revenir, au lieu d'avoir un bouton de retour intégré dans la page comme les autres pages détail d
- Correction validee : Ajouter backHref="/admin/users" à l'appel PageHeader ligne 78-81 du fichier src/app/(app)/admin/audit/page.tsx. Syntaxe : &lt;PageHeader title="Journal d'audit" description="..." backHref="/admin/users" />

## 45. [P2-navigation] Pas de chemin de retour vers l'administration
- Source : parcours navigation ; fichier : src/app/(app)/admin/corbeille/page.tsx
- Scenario : Admin gère la corbeille (soft-deletes). Pas de PageHeader avec backHref. Doit revenir par le menu sidebar.
- Correction proposee : Ajouter backHref="/admin/users" ou afficher un chemin de retour cohérent dans PageHeader.
- Statut : CONFIRME
- Contre-expertise : C:/Projects/outil-gestion-chantier/src/app/(app)/admin/corbeille/page.tsx:68-71 — PageHeader est présent mais sans prop backHref. Le composant PageHeader (C:/Projects/outil-gestion-chantier/src/components/ui/PageHeader.tsx:12,18-26) supporte le backHref et l'affiche avec un lien "Retour", mais la page corbeille ne le fournit pas. Comparé aux pages détail (chantiers/[id]/page.tsx:133 qui a backHref
- Correction validee : Ajouter backHref="/admin/users" (ou "/admin" si page maître) au PageHeader de C:/Projects/outil-gestion-chantier/src/app/(app)/admin/corbeille/page.tsx ligne 68. Exemple: PageHeader title="Corbeille" description="..." backHref="/admin/users" — cela affichera un lien "Retour" en haut pour revenir à la gestion des utilisateurs admin.

## 46. [P2-navigation] Pas de chemin de retour vers l'administration racine
- Source : parcours navigation ; fichier : src/app/(app)/admin/users/page.tsx
- Scenario : Admin manage les utilisateurs. PageHeader sans backHref. Contrairement aux écrans détail de ressources (ouvrier detail, commande detail), cette page n'a pas de lien de retour.
- Correction proposee : Ajouter backHref optionnel pour cohérence (ex: backHref="/admin" ou rien si c'est la page principale admin).
- Statut : CONFIRME
- Contre-expertise : C:/Projects/outil-gestion-chantier/src/app/(app)/admin/users/page.tsx:53 - PageHeader est appelé SANS le paramètre backHref. Contrairement aux pages détail (ex: C:/Projects/outil-gestion-chantier/src/app/(app)/ouvriers/[id]/page.tsx:131 avec backHref="/ouvriers", C:/Projects/outil-gestion-chantier/src/app/(app)/commandes/[id]/page.tsx:51 avec backHref="/commandes"), la page maître admin n'offre pa
- Correction validee : Ajouter backHref="/dashboard" au PageHeader ligne 53 de C:/Projects/outil-gestion-chantier/src/app/(app)/admin/users/page.tsx. Remplacer `<PageHeader title="Administration — Utilisateurs" description={...} />` par `<PageHeader title="Administration — Utilisateurs" description={...} backHref="/dashboard" />`. Cela offre un chemin de retour cohérent avec les autres pages de l'application et respecte

## 47. [P2-navigation] Pas de chemin de retour vers l'administration
- Source : parcours navigation ; fichier : src/app/(app)/exports/page.tsx
- Scenario : Admin télécharge les exports. PageHeader sans backHref. Doit naviguer depuis le menu ou bouton arrière.
- Correction proposee : Ajouter backHref="/admin/users" ou backHref="/parametres" pour incohérence minimale.
- Statut : CONFIRME
- Contre-expertise : C:/Projects/outil-gestion-chantier/src/app/(app)/exports/page.tsx:23-26 — PageHeader utilisé sans backHref. Composant PageHeader (src/components/ui/PageHeader.tsx:12,18-26) accepte bien la prop backHref et l'affiche en tant que lien "Retour", mais celle-ci n'est pas fournie sur la page exports. Utilisateur doit utiliser le bouton retour du navigateur — non conforme à l'UX attendue.
- Correction validee : Ajouter backHref="/admin/users" au composant PageHeader ligne 23 de src/app/(app)/exports/page.tsx : &lt;PageHeader title="Exports" description="..." backHref="/admin/users" /&gt;

## 48. [P2-navigation] Pas de chemin de retour vers l'administration
- Source : parcours navigation ; fichier : src/app/(app)/parametres/page.tsx
- Scenario : Admin configure l'app. PageHeader sans backHref. Pas de bouton retour visible.
- Correction proposee : Ajouter backHref optionnel. Envisager PageHeader avec backHref="/admin/users" ou laisser sans si page maître.
- Statut : REJETE
- Contre-expertise : /parametres/page.tsx:22-25 contient déjà un PageHeader avec titre "Paramètres" et description "Configuration de l'application". Le constat 48 suppose qu'il n'y a "pas de bouton retour visible", ce qui est factuellement faux — le PageHeader est rendu et visible. De plus, /parametres est une page maître (au même niveau que /dashboard, /planning dans la hiérarchie), donc selon le constat 49 elle doit

## 49. [P2-navigation] Pages maîtres sans PageHeader — absence de navigation breadcrumb
- Source : parcours navigation ; fichier : src/app/(app)/dashboard/page.tsx, src/app/(app)/profil/page.tsx, src/app/(app)/planning/page.tsx
- Scenario : Utilisateur navigue. Dashboard, Profil, Planning n'ont pas de PageHeader. Sans contexte de breadcrumb, l'utilisateur peut se perdre. Comparé aux listes (Chantiers, Ouvriers), ces pages manquent une entrée unifiée.
- Correction proposee : Ajouter PageHeader avec titre (sans backHref, car ce sont des pages maîtres). Exemple : PageHeader title="Tableau de bord" pour cohérence visuelle et accessibilité. Optionnel : ajouter description courte.
- Statut : REJETE
- Contre-expertise : Le constat affirme que Dashboard, Profil et Planning n'ont pas de PageHeader. Vérification du code actuel : profil/page.tsx ligne 33 contient `<PageHeader title="Mon profil" />`, planning/page.tsx lignes 151-154 contient `<PageHeader title="Planning" />`. Seul Dashboard n'a pas PageHeader (mais dispose d'un en-tête custom équivalent ligne 259-265). Le constat repose sur une lecture erronée ou une 

## 50. [P3-confort] Création d'utilisateur : champ Role bien placé, mais absence de confirmation du rôle à l'approbation
- Source : parcours admin ; fichier : src/app/(app)/admin/users/CreateUserBox.tsx
- Scenario : Admin crée un utilisateur CHEF. Lien d'invitation est généré. Utilisateur s'inscrit (pas d'étape de confirmation de rôle au sign-up). Admin approuve le compte. À aucun moment on ne redemande 'Tu es sûr que c'est un CHEF ?'
- Correction proposee : Comportement actuel est correct : le rôle est choisi à la création, pas changeable par l'invité. Mais documenter clairement à la création que le rôle est final et peut être changé après via la liste utilisateurs (liste admins comprend ça, c'est bon).
- Statut : CONFIRME
- Contre-expertise : Le code confirme l'absence de confirmation du rôle à l'approbation : - approveUser() (actions.ts:29-48) : met juste à jour status: "ACTIVE" sans vérifier ni afficher le rôle - UserActions.tsx (ligne 204-214) : bouton Approuver déclenche directement onApprove(userId) sans modale - Aucune UI de confirmation du rôle avant approbation dans UserActions.tsx - Le rôle est visible dans la liste (page.tsx:
- Correction validee : Ajouter une modale de confirmation avant approbation qui affiche le rôle sélectionné. Dans UserActions.tsx ligne 206-214, remplacer le onClick par un onClick qui affiche d'abord une DialogConfirm : "Approuver le compte pour [NOM] avec le rôle [ROLE] ?" avec boutons Annuler/Confirmer. L'action approveUser ne s'exécute qu'après confirmation explicite.

## 51. [P3-confort] Approbation d'un compte : processus correct mais pas de récap des données avant validation
- Source : parcours admin ; fichier : src/app/(app)/admin/users/actions.ts
- Scenario : Admin examine la liste utilisateurs, clique 'Approuver' pour un compte PENDING. Statut passe à ACTIVE, utilisateur peut se connecter. Mais l'admin n'a pas eu un écran de confirmation résumant : nom, email, rôle, chantiers (si CLIENT).
- Correction proposee : À titre informatif : laisser le flow actuel (bouton direct), mais enrichir l'UI /admin/users pour afficher le rôle + chantiers assignés avant l'approbation en un coup d'oeil (ce qui existe déjà de facto).
- Statut : CONFIRME
- Contre-expertise : Le constat est confirmé mais avec nuances. Dans page.tsx:103 et UserActions.tsx:205-214, le rôle et l'email sont DÉJÀ affichés dans la liste avant approbation. CEPENDANT, l'approbation se fait par un simple clic sans modale de confirmation (UserActions.tsx:208), et surtout les chantiers assignés (critiques pour un CLIENT) ne sont PAS visibles dans la liste avant approbation — ils ne sont accessibl
- Correction validee : Ajouter un petit badge ou une ligne supplémentaire sous le nom de l'utilisateur PENDING affichant les chantiers assignés (ex: « Chantiers : Chantier A, Chantier B »). Optionnellement, remplacer le clic direct "Approuver" par une modale rappelant : nom, email, rôle, chantiers avec boutons [Annuler] [Approuver].

## 52. [P3-confort] Paramètres de paie bien organisés et accessibles — pas de défaut
- Source : parcours admin ; fichier : src/app/(app)/parametres/page.tsx
- Scenario : Admin accède à /parametres, ajuste les jours/mois, mode de paiement par défaut. L'interface est claire et responsive (grid col-span).
- Correction proposee : Aucun défaut identifié. Mettre à jour le message d'info pour clarifier que les paiements existants ne sont pas recalculés.
- Statut : REJETE
- Contre-expertise : Constat #52 affirme qu'il n'existe "aucun défaut identifié" pour les paramètres de paie. C'est une constatation POSITIVE, non un défaut. Le code actuel (src/app/(app)/parametres/page.tsx, lignes 27, 53, 114-134) confirme : (1) interface responsive avec grid col-span mobile-first, (2) champs clairs et organisés, (3) message d'info DEJA PRESENT rappelant que les paiements existants ne sont pas recal

## 53. [P3-confort] Corbeille bien conçue avec rétention et purge auto — pas de défaut
- Source : parcours admin ; fichier : src/app/(app)/admin/corbeille/page.tsx
- Scenario : Admin consulte /admin/corbeille, voir tâches/commandes/rapports supprimés (30j de rétention). Peut restaurer ou purger avant l'expiration.
- Correction proposee : Pas de défaut trouvé. Excellent design.
- Statut : CONFIRME
- Contre-expertise : Code : C:/Projects/outil-gestion-chantier/src/app/(app)/admin/corbeille/page.tsx:10 (RETENTION_DAYS=30), page.tsx:38 (autoPurgeExpired), actions.ts:139-158 (purge auto avec nettoyage photos). Interface affiche jours restants (page.tsx:20-25), restauration possible (actions.ts:22-83), actions auditées. Aucun défaut détecté.
- Correction validee : Aucune correction nécessaire. Le design de la corbeille est excellent : rétention 30j, purge auto, nettoyage des photos lors de la purge, interface claire avec affichage des jours restants, restauration possible, audit logging complet.

## 54. [P3-confort] Journal d'audit avec filtres et pagination — bien implémenté, pas d'actions destructives
- Source : parcours admin ; fichier : src/app/(app)/admin/audit/page.tsx
- Scenario : Admin consulte l'audit, filtre par action/entity, voir la trace complète des paiements/suppressions/approbations.
- Correction proposee : Pas de défaut trouvé. Interface suffisamment claire.
- Statut : CONFIRME
- Contre-expertise : Code C:/Projects/outil-gestion-chantier/src/app/(app)/admin/audit/page.tsx (lignes 84-142 pour filtres, 198-229 pour pagination, 80 pour mention "Lecture seule"). Le journal d'audit implémente les filtres par action/entity, la pagination, et n'expose aucune action destructive. Page en lecture seule conforme au constat.
- Correction validee : Aucune correction requise. Le constat affirme correctement qu'il n'y a pas de défaut et l'interface est suffisamment claire. Aucune action n'est nécessaire.

## 55. [P3-confort] Messagerie hub : accès bloqué aux clients (ils sont redirects vers dashboard)
- Source : parcours chef ; fichier : src/app/(app)/messagerie/page.tsx (line 49)
- Scenario : Le client (role CLIENT) ne peut PAS voir la messagerie, seulement les rapports/incidents via le dashboard. Mais en doctrine, c'est une app de TERRAIN : les clients devraient voir le fil général du chantier (lisible only). DEFAUT : isolation trop forte, client ne voit rien du jour en temps réel.
- Correction proposee : Créer une messagerie lecture-seule pour les clients (role=CLIENT, messages affichés en view-only, pas de composer). Ou rediriger non pas vers /dashboard mais vers /messagerie avec filtrage read-only.
- Statut : CONFIRME
- Contre-expertise : src/app/(app)/messagerie/page.tsx:51-54 et src/app/(app)/messagerie/[chantierId]/page.tsx:40 contiennent des redirects explicites "if (me.isClient) redirect("/dashboard")". Clients sont complètement bloqués de la messagerie. Le composant ChantierFeed EXISTE et supporte l'affichage read-only (props viewerRole, canEdit), mais n'est jamais appelé pour les clients car la page redirige avant le rendu.
- Correction validee : Créer une vue messagerie lecture-seule pour les clients : 1) Supprimer le redirect ligne 40 du [chantierId]/page.tsx. 2) Passer viewerRole=me.role et canEdit=false au ChantierFeed (déjà fait aux lignes 365-366). 3) Masquer le ChantierComposer pour les clients (ajouter condition !me.isClient ligne 376). 4) Optionnel : bloquer le hub /messagerie pour clients (garde le redirect ligne 51-54 du page.ts

## 56. [P3-confort] Photos : upload n'affiche pas l'aperçu côté client (newFiles juste affiche le nom)
- Source : parcours chef ; fichier : src/app/(app)/incidents/IncidentForm.tsx + RapportForm.tsx
- Scenario : BEN prend 3 photos d'un incident (dégâts, situation). Les photos sont en attente d'upload. Il voit juste les NOMS des fichiers dans une grille (Camera + nom). Il NE VOIT PAS les images pour confirmer qu'il a les bonnes. DEFAUT : manque de feedback visuel = risque de se tromper de photo.
- Correction proposee : Créer un miniature aperçu (préview) avec URL.createObjectURL côté client ET bien nettoyer (revokeObjectURL) après submit. Ou afficher un placeholder + nom.
- Statut : CONFIRME
- Contre-expertise : IncidentForm.tsx:182-190 affiche uniquement {f.name} + icône Camera pour les photos en attente. RapportForm.tsx:225-238 affiche le même pattern avec commentaire explicite ligne 230-231: "Pas d'aperçu URL.createObjectURL ici pour éviter les fuites mémoire". Or, le code de MaterielForm.tsx:44 + 81-88, OuvrierForm.tsx:62 + 96-103, et ReserveForm.tsx:90 + 261-273 utilisent tous URL.createObjectURL pou
- Correction validee : Implémenter l'aperçu d'image dans IncidentForm.tsx et RapportForm.tsx en utilisant URL.createObjectURL comme dans ReserveForm.tsx (ligne 90 pour la création du preview, lignes 262-273 pour l'affichage). Nettoyer les URLs au reset du formulaire (form.reset() en ligne 107 de ReserveForm). Cela élimine le risque de fuite mémoire tout en fournissant le feedback visuel manquant au chef (BEN).

## 57. [P3-confort] Pointage plage : pas de raccourci clavier pour appliquer rapidement (TAB, ENTER, etc.)
- Source : parcours chef ; fichier : src/app/(app)/ouvriers/MultiPointageForm.tsx
- Scenario : BEN utilise le mode plage (calendrier), veut pointer 10 jours pour 1 ouvrier. Il doit cliquer le jour dans le calendrier, puis sélectionner l'option (0.5 ou 1 jour), puis soumettre. Chaque jour = 2-3 clics. DEFAUT : pas de pattern rapide (ex: cliquer deux fois = 1 jour, une fois = 0.5).
- Correction proposee : Implémenter un mode 'rapide' du calendrier : clic = basculer entre états (Absent → 0.5 → 1 → Absent). Ou touches clavier (1, 5, 0 = 1j / 0.5j / absent).
- Statut : REJETE
- Contre-expertise : Le constat décrit un flux de "cliquer le jour, puis sélectionner l'option (0.5 ou 1 jour), puis soumettre = 2-3 clics par jour", mais le PointageCalendar implémente déjà exactement le pattern "rapide" proposé : 1 clic = cycle automatique entre états (0 → 1 → 0.5 → 0). Code : src/app/(app)/pointage/PointageCalendar.tsx ligne 184-190 (fonction cycle) et ligne 388 (onClick). Le constat confond deux U

## 58. [P3-confort] Bouton 'Nouveau rapport' visible pour CLIENT sur la fiche chantier mais caché par conditions
- Source : parcours client ; fichier : src/app/(app)/rapports/RapportsSection.tsx (lignes 84-96)
- Scenario : CLIENT voit la fiche chantier /chantiers/{id}. RapportsSection affiche des rapports. Bouton 'Nouveau rapport' (ligne 87) n'est affiché que si isAdmin=true (ligne 84 contrôle l'affichage). Mais : sur les chantiers, non-admins (CLI ENT = isAdmin false) ne voient pas le bouton. C'est juste mais le contrôle est côté component, pas au routing → risque de fuite si composant réutilisé.
- Correction proposee : Ajouter un commentaire JSDoc expliquant que RapportsSection est CLIENT-safe (lecture seule). Tester mobile : le bouton '+' du chantier ne doit pas proposer 'créer rapport' pour CLIENT.
- Statut : REJETE
- Contre-expertise : C:\Projects\outil-gestion-chantier\src\app\(app)\rapports\RapportsSection.tsx:84-96 — Le constat affirme que le bouton "Nouveau rapport" n'est affiché que si isAdmin=true et que "la ligne 84 contrôle l'affichage". C'est une FAUSSE LECTURE du code. La ligne 84 teste uniquement `{!adding &&` (l'état de création), pas isAdmin. Le prop isAdmin est reçu ligne 71 mais n'est JAMAIS utilisé dans le compos

## 59. [P3-confort] Groupe 'Chantiers & équipes' contient 'Ouvriers' (tarifs = jargon + financier) mais n'est pas caché pour CLIENT
- Source : parcours client ; fichier : src/components/NavSidebar.tsx (lignes 86-91)
- Scenario : CLIENT ne voit pas 'Ouvriers' dans la sidebar (clientHidden: true, ligne 90). Correct. Mais 'Équipes' est visible (ligne 88, pas clientHidden). CLIENT clique sur 'Équipes' → page liste d'équipes avec infos brutes (noms, count ouvriers, affectations). Pas de montants, mais termes 'équipe' + 'ouvrier' peuvent confondre un maître d'ouvrage (attendrait 'Personnel' ou 'Ressources humaines').
- Correction proposee : Masquer 'Équipes' pour CLIENT via clientHidden: true (ligne 88). Ou : si CLIENT doit voir équipes, clarifier le label → 'Équipes affectées' et contexte = 'assignations du personnel à vos chantiers'.
- Statut : REJETE
- Contre-expertise : src/components/NavSidebar.tsx:88 - Le lien "Équipes" a déjà `clientHidden: true` appliqué depuis l'introduction du rôle CLIENT. Le filtrage à ligne 436 (`(!it.clientHidden || !isClient)`) s'applique correctement et empêche l'affichage pour les CLIENTs. L'historique git montre que ce flag a toujours existé depuis le commit d001390 qui introduit le rôle CLIENT. Le constat décrit un état de code qui 

## 60. [P3-confort] CLIENT voit 'Rapports hebdomadaires reçus' mais pas clair qu'il faut déjà avoir reçu un rapport (envoyeAuClient=true)
- Source : parcours client ; fichier : src/app/(app)/dashboard/ClientDashboard.tsx (lignes 174-189)
- Scenario : CLIENT ouvre le dashboard. Aucun rapport hebdo affiché (section vide, ligne 166). CLIENT lit 'Rapports hebdomadaires reçus' mais rien. Pense : 'Pourquoi pas de rapport? Suis-je censé voir quelque chose?' Message d'empty state manquant ou pas clair que ADMIN doit 'envoyer' le rapport d'abord.
- Correction proposee : Si rapportsHebdoRecents.length === 0, afficher un message doux : 'Les rapports sont envoyés chaque semaine par l'équipe pilotage' au lieu de rien. Ou ajouter une section d'info sur rapports-hebdo.
- Statut : CONFIRME
- Contre-expertise : Fichier: C:/Projects/outil-gestion-chantier/src/app/(app)/dashboard/ClientDashboard.tsx, lignes 166-211. Code: `{rapportsHebdoRecents.length > 0 && (...)` — la section "Rapports hebdomadaires reçus" ne s'affiche que si des rapports existent (filtrés par envoyeAuClient: true, ligne 61). Quand rapportsHebdoRecents.length === 0, AUCUN message ou empty state n'est affiché. Le CLIENT ne voit rien et ne
- Correction validee : Ajouter après la section des rapports (ligne 211) une condition alternative: `{rapportsHebdoRecents.length === 0 && chantiers.length > 0 && (...Card avec EmptyState affichant "Les rapports hebdomadaires sont envoyés chaque semaine par l'équipe pilotage" ou message similaire)}`. Ou : afficher TOUJOURS la Card mais avec EmptyState si length === 0, plutôt que de la cacher entièrement avec &&.

## 61. [P3-confort] Formulaires de clôture : pas de retour clair au conducteur sur le succès de l'action
- Source : parcours conducteur ; fichier : src/app/(app)/locations/[id]/page.tsx + src/app/(app)/demandes/[id]/page.tsx
- Scenario : Conducteur clôt une location (retour du matériel), remplit date + note, clique « Clôturer ». Page se raffraîchit. Pas de toast/notification que c'est ok. Doit scroll pour voir la nouvelle section « Récapitulatif » pour confirmer qu'elle a été clôturée.
- Correction proposee : 1) Ajouter useToast().success(« Location clôturée ») après revalidatePath. 2) Faire un scroll vers la section mise à jour ou afficher un encart vert en haut « ✓ Clôturée le ... ».
- Statut : CONFIRME
- Contre-expertise : Fichier: C:/Projects/outil-gestion-chantier/src/app/(app)/locations/actions.ts lignes 90-137 - La fonction `cloturerLocation` appelle `revalidatePath()` mais ne contient aucun appel à `toast()` ou redirection. Pas de feedback utilisateur immédiat. Le conducteur doit scroller pour voir le badge "Clôturée" ou la section "Récapitulatif" pour confirmer le succès. Composant Toast existe (src/components
- Correction validee : Convertir le formulaire de clôture en Client Component avec useTransition(), appeler startTransition() au submit, puis afficher useToast().success("Location clôturée") après le succès. Ou ajouter un redirect vers la même page avec ?success=1 et afficher un toast depuis un Client Component wrapper. Actuellement aucun feedback visuel n'est affiché au conducteur.

## 62. [P3-confort] Actions message cachées au survol (refuser demande) — contexte messagerie OK
- Source : parcours navigation ; fichier : src/app/(app)/messagerie/ChantierFeed.tsx
- Scenario : Utilisateur terrain voit un message avec action 'Refuser cette demande'. Le bouton avec [@media(hover:hover)]:opacity-0 [@media(hover:hover)]:group-hover:opacity-100 n'apparaît qu'au survol. Exception acceptable car c'est la messagerie (contexte critique, actions rares).
- Correction proposee : Statut: ACCEPTÉ pour messagerie (contexte: actions contextuelles, @media(hover:hover) déjà utilise media query pour sauter les écrans tactiles). Aucune action requise.
- Statut : REJETE
- Contre-expertise : Le bouton "Refuser cette demande" est VISIBLE en permanence, pas caché au survol. Code: src/app/(app)/messagerie/ChantierFeed.tsx ligne 911 affiche le bouton Refuser() directement sans hover-only. Seuls les boutons du footer d'actions (ligne 944 : Réagir, Voir détail, Cacher client, Supprimer) utilisent [@media(hover:hover)]:opacity-0 group-hover:opacity-100. Le constat confond deux zones distinct

## 63. [P3-confort] Vocabulaire incohérent: 'demande à valider' vs 'Approuver et commander'
- Source : parcours navigation ; fichier : src/app/(app)/dashboard/QuickActionsBar.tsx, src/app/(app)/dashboard/page.tsx
- Scenario : Utilisateur voit deux labels différents pour une même action (approuver une demande): 'demande à valider' (dashboard), 'Approuver cette demande' (messagerie), 'Approuver &amp; commander' (feed). Manque de cohérence terminologique.
- Correction proposee : Standardiser le vocabulaire: choisir SOIT 'valider' SOIT 'approuver'. Recommandation: 'Approuver' (plus métier que 'valider' pour les demandes). Remplacer 'à valider' par 'à approuver' partout.
- Statut : CONFIRME
- Contre-expertise : Incohérence confirmée : - Dashboard (src/app/(app)/dashboard/page.tsx:194) : "de matériel à valider"  - ChantierFeed (src/app/(app)/messagerie/ChantierFeed.tsx:902) : "Approuver & commander" - ChantierComposer (src/app/(app)/messagerie/ChantierComposer.tsx:71) : "Demande à approuver puis commander"  Trois labels différents (valider/approuver) pour une même action métier : approuver une demande de 
- Correction validee : Standardiser le vocabulaire en utilisant "Approuver" partout (meilleur terme métier que "valider"). 1. src/app/(app)/dashboard/page.tsx:194 - remplacer "de matériel à valider" par "de matériel à approuver" et chip "valider" par "approuver" 2. Vérifier que ChantierFeed.tsx et ChantierComposer.tsx utilisent déjà "approuver" (c'est le cas). Cela harmonisera l'UX sur tous les parcours utilisateur.
